fastapi==0.115.4
uvicorn[standard]==0.30.6
pydantic==2.8.2
pydantic-settings==2.4.0
duckdb==1.5.4
pandas==2.3.3  # mlflow-skinny 3.14.0 requires pandas<3 - keep below 3.0.x here specifically
mlflow-skinny==3.14.0
scikit-learn==1.7.2
httpx==0.27.2
PyJWT==2.12.1
# mlflow's S3ArtifactRepository (used to download the champion model's
# artifacts from s3://gold via MinIO) imports boto3/botocore directly - not
# a transitive dependency of mlflow-skinny, which has no S3 support built in.
boto3==1.35.0
# mlflow.sklearn.log_model()'s default serialization_format is "skops" (not
# pickle) - the training side (airflow image) has it as a transitive dep of
# something else, but mlflow-skinny here doesn't pull it in, so loading any
# champion model fails with ModuleNotFoundError: No module named 'skops'.
skops==0.14.0
