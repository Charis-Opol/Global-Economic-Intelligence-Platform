# Frontend (placeholder)

This container currently only proves the build pipeline works end to end.

Full scaffold — Vite + React + TypeScript + Tailwind + shadcn/ui, routing,
auth pages, dashboard layout — is generated in **Day 3, Step 1**, as its own
milestone. Wiring it in now, before the backend contracts exist, would mean
throwing most of it away later.
