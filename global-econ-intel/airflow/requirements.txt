apache-airflow-providers-celery==3.7.2
apache-airflow-providers-postgres==5.11.3
apache-airflow-providers-amazon==8.25.0
apache-airflow-providers-http==4.12.0
requests==2.32.3
pydantic==2.8.2
pydantic-settings==2.4.0
tenacity==8.5.0
boto3==1.35.0
pandas==2.3.3  # mlflow 3.14.0 requires pandas<3 - keep below 3.0.x here specifically
great_expectations==1.19.0
duckdb==1.5.4
mlflow==3.14.0
scikit-learn==1.7.2
# Pinned to match spark-master/spark-worker's apache/spark:3.5.3 exactly -
# a submitting client's Spark version needs to match the cluster's, and this
# is what lets the ingestion DAGs run the Bronze->Silver ETL step themselves
# (spark-submit against spark://spark-master:7077) instead of that being a
# separate manual step.
pyspark==3.5.3
